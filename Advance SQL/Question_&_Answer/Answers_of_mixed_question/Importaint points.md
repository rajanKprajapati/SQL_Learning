WHERE filters → makes a temporary group
AVG() runs on that filtered data
Returns one value → scalar subquer
.....................................
👉 “If subquery returns one value → normal comparison works”
👉 “If it returns many → use IN / ANY / ALL”
.......................................
